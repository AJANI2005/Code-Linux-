
/**
 * 
 * @param {string[]} array
 */
const perm = (array)  => {
    if (array.length == 1){
       return array
    }else{
        const perms = []
        array.forEach((element) => {
           perms.append(element + ) 
        })
    }
}

// string to array
console.log(perm("abc".split("")))

