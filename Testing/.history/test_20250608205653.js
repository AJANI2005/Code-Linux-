
/**
 * 
 * @param {string} str
 */
const perm = (str)  => {
    if (str.length == 1){
       return [str]
    }else{
        const perms = []
        str.
        return perms;
    }
}

// string to array
console.log(perm("abc"))

